One-line: The master play/pause button — the single element that always carries the full twilight gradient and accent glow.

```jsx
<PlayButton playing={isPlaying} size={64} onClick={toggle} />
```

Sits in the bottom playback bar. Scale with `size`. Host must run `lucide.createIcons()`.
