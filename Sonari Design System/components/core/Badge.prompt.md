One-line: A small pill for status, counts and preset/category labels.

```jsx
<Badge variant="accent" icon="zap">Tile</Badge>
<Badge variant="gradient">Active</Badge>
```

Variants: `neutral`, `accent` (soft violet), `gradient` (full twilight), `success`, `outline`.
