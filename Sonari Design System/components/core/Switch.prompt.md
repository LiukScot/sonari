One-line: A settings on/off toggle; the track fills with the twilight gradient when on.

```jsx
<Switch checked={fade} onChange={setFade} />
```

Used in Settings rows (fade in/out, auto-resume, etc.).
